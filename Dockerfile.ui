FROM python:3.11-slim
WORKDIR /app
RUN pip install --no-cache-dir streamlit requests
COPY ui/StreamlitUI.py ./StreamlitUI.py
EXPOSE 8501
CMD ["streamlit", "run", "StreamlitUI.py", "--server.address", "0.0.0.0"]
